// bitwise operator--used to work on binary data  of the numbers

// why bitwise operator is used--because arithmetic calcculations and many moe etc because computer understand binary so it works on binary only
let a = 5;
let b =3;
// and operator in bitwise
console.log(a&b);

// or operator in bitwise
console.log(a|b);

// xor operator in bitwise
console.log(a^b);
 
// left shift operator in bitwise
console.log(a<<b);

// right shift operator in bitwise
console.log(a>>b);

// used this bitwise in  blockchain,finance,networking ,swiggy zomato payment gateway
   
let curr = 100000000;
